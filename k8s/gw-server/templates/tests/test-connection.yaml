apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gw-server.fullname" . }}-test-connection"
  labels:
    {{- include "gw-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gw-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

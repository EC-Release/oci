apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent+vln.fullname" . }}
  labels:
    {{- include "agent+vln.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent+vln.selectorLabels" . | nindent 4 }}

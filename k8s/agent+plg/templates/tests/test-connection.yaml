apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agent+tls.fullname" . }}-test-connection"
  labels:
    {{- include "agent+tls.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agent+tls.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
